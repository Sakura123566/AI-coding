# BUG-01 整改计划（仅过目，待你拍板后执行）

> 现象（队友报告）：输入「超快成像」等**不在翻译对照表内**的中文主题，返回论文大量与主题无关（混入 Plantaris Tendon Rupture / 跖肌腱断裂诊断 等完全不相关记录）。
> 状态：本文档只给方案与权衡，**未改动任何代码**。是否改、怎么改，由你决定。

---

## 一、根因（已读 `backend/llm/translate.py` + `backend/pipeline.py` 确认）

检索链路：
`pipeline._run_research` → `resolve_keyword()` 先做「中文→英文」翻译 → 再把英文关键词丢给 Crossref 检索。

翻译只有两条路，且**都依赖外部条件**：

1. `KEYWORD_MAP` 硬编码对照表（约 30 词）——只覆盖少量常见词；
2. 调大模型翻译——要求 `llm_provider == "openai"` **且** `llm_api_key` 已配置。

「超快成像」**不在对照表**，且当前 `LLM_PROVIDER=mock`、无 Key → 翻译失效 → `resolve()` 返回**原样中文** → 中文串直接丢给 Crossref → 中文查询乱匹配，捞回一堆不相关记录。

代码自身注释即警告此风险（`translate.py` 开头：「不翻译…会捞回一堆不相关的中文水刊」）。

### 简易判据（页面侧可自查）
- 翻译成功时，页面会显示灰条「已按「xxx」检索」；
- **无此灰条 = 未翻译**（「超快成像」即无灰条，验证翻译失效）。

---

## 二、影响范围

- 仅影响**中文主题 + 不在 KEYWORD_MAP 内的词**；
- 英文关键词（如 `ultrafast imaging`）走 Crossref 正常，不受影响；
- 与「智能体对话只会重复一句话」是**同一瓶颈**：两者都卡在「后端没有真 LLM」。配了 Key，翻译与对话会**同时**变正常。

---

## 三、修复选项（三选一或组合，供你定）

| 选项 | 做法 | 成本 | 效果 | 风险/代价 |
|---|---|---|---|---|
| **A. 配真 LLM Key** | 在 `backend/.env`（或环境变量）设 `LLM_PROVIDER=openai`、`LLM_API_KEY=...`（可选 `LLM_BASE_URL`/`LLM_MODEL`，默认 `gpt-4o-mini`） | 低（改配置） | 翻译 + 对话**同时**解决，最彻底 | 需可用的 OpenAI 兼容密钥；有调用费用 |
| **B. 扩充 KEYWORD_MAP** | 在 `translate.py` 的对照表里加词（如 超快成像→ultrafast imaging 等） | 低（纯加数据） | 仅覆盖手动加的词，表内中文正常 | 无法覆盖长尾；需持续维护词表；不治本 |
| **C. 增加国内论文源** | 除 Crossref 外接入 CNKI/万方/ Semantic Scholar 等，中文可直接命中 | 高（接新数据源 + 适配字段） | 中文主题召回改善，不依赖翻译 | 工作量大；部分源需授权/接口 |

---

## 四、规避技巧（不改代码也能用）
表外中文主题 → 直接输**英文关键词**（如 `ultrafast imaging`），绕过中译英失真，立即得到相关结果。

---

## 五、我的建议（仅供参考，不执行）
- 若你手头有 OpenAI 兼容 Key：**优先 A**，一次解决翻译 + 对话两个症状，投入最小。
- 若暂时没有 Key：先用 **B** 把高频词补上（止血），等 Key 到位再上 A。
- **C** 单独立项，不阻塞当前问题。

---

## 六、待你确认
1. 是否要修？修哪个（A / B / C / 组合）？
2. 若选 A：Key 由你提供，还是用你自有后端地址（之前你说「后端库里自己有」，当前 `.env` 已含 `VITE_USE_MOCK=false`+`VITE_API_BASE`，但**没有 LLM_PROVIDER/LLM_API_KEY`）？
3. 若选 B：给一份「希望优先覆盖的高频词」清单，或授权我按常见科研主题先补一版。
