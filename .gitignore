# 密钥绝不进仓库
.env
*.env
!\.env.example

# 用户数据（SQLite：账号、会话、记忆、画像）
backend/data/

# 论文搜索缓存 + arXiv 限速状态（默认 backend/data/paper_cache.db，不进仓库）
*.db
*.db-wal
*.db-shm

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.log

# 前端（队员2 的目录）
node_modules/
dist/
# 本地多模式构建产物（mock / real 对比备份，非源码，勿提交）
dist_*_build/

# 编辑器
.vscode/
.idea/
.DS_Store

# 前端部署产物（沙箱 CloudStudio 构建用，非源码）
web-dist/
web-dist-*/
