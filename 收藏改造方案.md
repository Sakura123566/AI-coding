# 收藏改造方案（先过目，确认后再改）

> 目标：给「收藏」加两层能力——① **收藏夹（分组/空间）**；② **给收藏论文自定义打标签**。
> 状态：仅方案，未改代码。两个关键岔路见文末「待确认」。

---

## 一、现状（已读代码确认）
- 收藏数据：`stores/knowledgeParty.ts` 里 `favorites: Record<paperId, Paper>`，**全局一个扁平表**，存 localStorage，纯本地、没接后端。
- 操作：`toggleFavorite(paper)` / `isFavorite(id)` / `clearFavorites()`。
- 展示：`FavoritesView.vue` = `store.favoriteList`（全部收藏）丢给 `PaperList.vue` 渲染网格卡片（卡片右上角星标一键收藏/取消）。
- 已有但**只管搜索历史**的「空间」：`spaces` / `addSpace` / `switchSpace`，与收藏无关。

---

## 二、目标数据模型（本地优先，localStorage）
每篇收藏论文带 `tags: string[]`。收藏按「收藏夹」分组。两种架构见「待确认 Q1」。

每篇收藏论文类型：
```ts
interface FavPaper extends Paper { tags: string[] }
```

---

## 三、UI 改动点
1. **收藏夹侧栏/标签（FavoritesView）**
   - 顶部或左侧：收藏夹列表（默认一个「我的收藏」）+「＋ 新建收藏夹」+ 重命名/删除。
   - 标签筛选条：展示所有出现过的标签 chip，点选即筛选当前收藏夹内论文。
   - 主区：`PaperList` 渲染当前收藏夹论文。
2. **论文卡片（PaperList）**
   - 卡片底部展示该论文的标签 chips。
   - 新增「打标签」入口（小标签图标 → 内联编辑：输入新标签回车添加、点 × 删除）。
   - 星标按钮：在收藏语境下，从「取消收藏」改为「从当前收藏夹移除」。
3. **搜索页星标**
   - 从搜索页点星标收藏时，归入**当前/默认收藏夹**（详见 Q1 行为）。

---

## 四、待确认（决定后才动手）
- **Q1 收藏夹架构**：
  - A（推荐，最省事，契合你说的"收藏夹空间"）：**复用现有「空间」**——每个空间自带一组收藏 + 搜索历史，切换空间即切换收藏夹。改动小，且和"不同的收藏夹空间"语义一致。
  - B（更清晰但多写些代码）：**新增独立「收藏夹」体系**，与搜索历史空间解耦，两者互不影响。
- **Q2 存储**：本地优先（localStorage，与现状一致，不接后端）还是接后端（需后端加收藏接口，目前后端无此端点且为 mock）？→ 推荐本地优先。
- **Q3 标签作用域**：全局标签（跨收藏夹，可按标签筛选）还是每收藏夹内独立？→ 推荐全局，便于跨收藏夹筛选。

---

## 五、预计改动文件
- `stores/knowledgeParty.ts`：收藏数据结构 + 收藏夹/标签相关方法。
- `components/FavoritesView.vue`：收藏夹侧栏 + 标签筛选。
- `components/PaperList.vue`：卡片标签展示 + 打标签编辑。
- （若选 A）`App.vue` 左栏空间切换已存在，顺带让收藏跟随空间。
- 不影响后端（本地优先前提下）。
