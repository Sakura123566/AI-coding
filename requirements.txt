# 主服务依赖保持最小；PDF 周报额外使用 ReportLab。
# 论文检索与模型调用全部用标准库 urllib 实现，不引入 openai / arxiv 等 SDK
fastapi>=0.110
uvicorn[standard]>=0.27

# 仅当论文 MCP 用 WebSocket 端点（ws://...）时需要
websocket-client>=1.7

# 周报 PDF（使用 ReportLab 内置中文 CID 字体，无需额外字体文件）
reportlab>=4.2,<5
